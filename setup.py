from setuptools import setup, find_packages
setup(
    name="topoy",
    version="0.1",
    packages=find_packages(),
    author="beezee",
    description="Abstract and encapsulate your dependencies, state management, data accumulation, and error handling",
    keywords="mtl, tagless",
    url="https://github.com/beezee/evalenv",
    project_urls={
        "Source Code": "https://github.com/beezee/evalenv",
    }
)
